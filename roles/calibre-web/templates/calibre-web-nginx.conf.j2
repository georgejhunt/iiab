location ~ ^/books {
   proxy_pass  http://127.0.0.1:{{ calibreweb_port }};
   proxy_set_header   X-Forwarded-For $remote_addr;
   proxy_set_header   Host $http_host;
   root /usr/local/calibre-web/cps;
}
